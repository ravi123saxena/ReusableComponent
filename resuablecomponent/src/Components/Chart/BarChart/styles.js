import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
        marginVertical: 8,
        borderRadius: 10,
        margin: 10,
    },
    graphStyle: {
        marginVertical: 8,
        borderRadius: 8,
    },

});