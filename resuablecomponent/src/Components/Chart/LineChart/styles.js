import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
      overflow:'visible',
      backgroundColor: '#fff',
      justifyContent:'center'
    }
  });