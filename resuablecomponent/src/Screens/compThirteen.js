import React from 'react';
import { View } from 'react-native';

export default CompThirteen = ({ navigation }) => {
      return (
        <View style={{flex:1}}>
         
        </View>
      );
};
