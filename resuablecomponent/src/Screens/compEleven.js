import React from 'react';
import { View } from 'react-native';

export default CompEleven = ({ navigation }) => {
      return (
        <View style={{flex:1}}>
          
        </View>
      );
};

