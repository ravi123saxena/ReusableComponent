import React from 'react';
import { View } from 'react-native';

export default CompTwelve = ({ navigation }) => {
    
      return (
        <View style={{flex:1}}>
         
        </View>
      );
};
